# Copy to .env and fill in. KEY=value, no quotes, no `export`. The real .env is
# gitignored — never commit secrets.

# Google Calendar (service account)
GOOGLE_PRIVKEY=
GOOGLE_SERVICE_EMAIL=
GOOGLE_CALENDAR_ID=
GOOGLE_TODO_CALENDAR_ID=

# FindMy
FINDMY_EMAIL=
FINDMY_DEVICE_ID=
FINDMY_AAS=
FINDMY_FCM_ID=
FINDMY_FCM_TOKEN=

# Weather (open-meteo)
WEATHER_LATITUDE=
WEATHER_LONGITUDE=
WEATHER_TIMEZONE=

# Telemetry / logging
LOG_IP=
LOG_PORT=
API_URL=

# LED strip (tuya)
LED_STRIP_ID=
LED_STRIP_NAME=
LED_STRIP_IP=
LED_STRIP_VERSION=
# _VERSION=(3.3|3.5)
LED_STRIP_KEY=
LED_STRIP_OPTIMISTIC=  
#  _OPTIMISTIC=(true|false)

# LED lamp (tuya)
LED_LAMP_ID=
LED_LAMP_NAME=
LED_LAMP_IP=
LED_LAMP_VERSION=
LED_LAMP_KEY=
LED_LAMP_OPTIMISTIC=

# Update frequencies (seconds)
WEATHER_UPDATE_FREQUENCY=
